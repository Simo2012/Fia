imports:
    - { resource: config.yml }

framework:
    validation:
        cache: apc

doctrine:
    orm:
        metadata_cache_driver: apc
        result_cache_driver: apc
        query_cache_driver: apc
    dbal:
        host: SCEAUBDDR7
        port: 5442
        dbname: sceau
        user: sceau_user
        password: hello

# Surcharge des paramètres
parameters:
    router.request_context.host: recette.fia-net.ccm
    router.request_context.scheme: http
    router.request_context.base_url: /