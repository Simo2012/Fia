{% extends 'SceauBundle:Extranet:extranet.html.twig' %}

{% block body %}
    {{ 'Les lignes du fichier CSV doivent respecter le format suivant : id, nom, prénom, email' | trans() }}
    <br>
    {{ form_start(form) }}

    {{ 'Étape 1 : indique le fichier à importer en cliquant sur le bouton Parcourir : ' | trans() }}
    {{ form_row(form.file) }}
    {{ form_row(form.filename) }}
    <br>
    {{ 'Étape 2 : cliquez sur le bouton Valider et patientez quelques instants, une page de confirmation va s\'afficher : ' | trans() }}
    <button type="submit"><span>{{ "Valider" | trans() }}</span></button>

    {{ form_end(form) }}
{% endblock %}